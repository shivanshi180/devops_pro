[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "devops_pro"
version = "1.0.0"
description = "FastAPI DevOps Project"
authors = [
  { name="Shivanshi Vaish" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []